#include<stdio.h>
int main()
{
    int d;
    printf("Enter a no.");
    scanf("%d",&d);
    if(d>0)
    {
        printf("%d is a positive no.",d);
      }
    else
    {
        printf("%d is a negative no.",d);
        }
    return 0;

}
